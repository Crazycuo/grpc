/*
 * it's an example by using makefile in Windows by atom_make
 *
 * it's better to type "make" in the DOS rather than using F6
 */
#include <iostream>

int main(int argc, char const *argv[]) {
  std::cout << "this is a test of Atom Makefile for Windows" << '\n';
  return 0;
}
